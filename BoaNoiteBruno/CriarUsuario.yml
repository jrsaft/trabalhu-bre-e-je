info:
  name: CriarUsuario
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/usuarios
  body:
    type: json
    data: |-
      {
        "name": "Duda",
        "surname": "Velho",
        "age": "30",
        "address": "fijdfjfd",
        "email": "eduarda.v@ienh.com.br"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
